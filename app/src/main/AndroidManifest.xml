<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arief.kamus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddData"
            android:label ="Add Data"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".Riwayat_Activity"
            android:label ="Riwayat"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".Penerjemah_Activity"
            android:label ="Penerjemah"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".DaftarKata_Activity"
            android:label ="Daftar Kata"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>